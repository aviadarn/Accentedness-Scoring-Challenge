# Comparison model records

This directory contains tracked configuration and metric records for rejected
model-capacity experiments. It is not the selected deployable model.

- `../model/` — selected, self-contained production checkpoint;
- `whisper-small/` — E02 comparison metadata; its large weight file is local and
  git-ignored.

See [E02](../../experiments/E02-whisper-small/) for the hypothesis, result, and
decision.
